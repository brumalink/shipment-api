# Brumalink Shipment API

Backend service for Brumalink temperature-controlled shipments.

Work in progress.

## Local development

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```
